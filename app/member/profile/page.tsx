import { requireApprovedMember } from "@/lib/member-session";
import { reportDb } from "@/lib/report-store";
import { ReportShell } from "@/components/report-shell";
import { ProfileChangeForm } from "@/components/profile-change-form";
export const dynamic="force-dynamic";
export default async function Page(){const member=await requireApprovedMember(),pending=Boolean(await reportDb().prepare("SELECT id FROM profile_change_requests WHERE member_id=? AND status='pending'").bind(member.id).first());return <ReportShell title="Company profile" description="Review your company identity and request updates to approved contact details."><section className="mb-6 rounded-2xl border bg-white p-6"><dl className="grid gap-5 sm:grid-cols-2"><Info label="User ID" value={member.loginId||"Not issued"}/><Info label="Company name" value={member.companyName}/><Info label="GSTIN" value={member.gstin}/><Info label="Category" value={member.category==="agriculture"?"Agriculture":member.otherCategory||"Other"}/><Info label="Location" value={`${member.taluka}, ${member.district}, ${member.state} ${member.pincode}`}/></dl><p className="mt-5 text-sm text-slate-500">Company name, GSTIN, category and location remain locked.</p></section><ProfileChangeForm member={{responsiblePersonName:member.responsiblePersonName,mobileNumber:member.mobileNumber,email:member.email,address:member.address}} pending={pending}/></ReportShell>}
function Info({label,value}:{label:string;value:string}){return <div><dt className="text-sm text-slate-500">{label}</dt><dd className="mt-1 break-words font-medium">{value}</dd></div>}
